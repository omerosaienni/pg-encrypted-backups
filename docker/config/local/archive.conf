# The source archives to the local demo stanza. The demo's migration step
# (ALTER SYSTEM) later points this at demo-encrypted - a file setting like this
# is overridable, a command-line -c option would not be.
archive_command = 'pgbackrest --stanza=demo archive-push %p'
